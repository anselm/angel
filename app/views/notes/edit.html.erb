<h1>Edit Note</h1>

<% form_for ( @note, :url => "/notes/update/#{@note.id}", :html => { :multipart => true } ) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => "form", :object => f %>
  <%= f.submit "Update" %>
<% end %>

